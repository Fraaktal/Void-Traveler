using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestValves : MonoBehaviour
{
    public EffectOnRotation Valve;

    // Start is called before the first frame update
    void Start()
    {
        Valve.StartGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
